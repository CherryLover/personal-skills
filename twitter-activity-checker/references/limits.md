# 限制

- 点赞数据是否可见，取决于目标账号公开状态、接口权限和风控情况。
- 某些账号即使主页可看，点赞接口也可能返回空结果或报错。
- 受保护账号、已删除推文、地区限制内容，脚本都可能拿不到。
- `likes.createdAt` 表示被点赞推文的发布时间，不代表点赞发生时间。
- 如果用户要求“最近”，默认先看前 1 到 3 页；超过这个范围时，优先用 `nextCursor` 继续分页，而不是一次性拉很多页。
